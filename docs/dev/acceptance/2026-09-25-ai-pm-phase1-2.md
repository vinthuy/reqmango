# Phase 1–2 验收清单（手测）

> **日期**: 2026-09-25  
> **范围**: AI 项目管理 Phase 1 + Phase 2（已合并 `master`）  
> **通过标准**: 下列每条 ☐ → ☑；阻塞 bug 记入本文件「缺陷」并修后再标过。  
> **非目标**: Harness / Loop / 多 Agent 流水线（已取消作产品方向）。

环境：本地或测试服；已登录；有项目成员权限；建议配置可用 AI Key（部分项无 LLM 会走离线/降级，需在备注注明）。

---

## Phase 1

| # | 验收项 | 步骤 | 期望 | 结果 |
|---|--------|------|------|------|
| P1-1 | 侧栏无 Agents 主入口 | 登录后看默认侧栏 | 无「AI Agents」一级项；高级/实验入口仍可达 `/agents` | ✅ code |
| P1-2 | Copilot Ask\|Build + 上下文 | Issue 页 Ctrl+J / FAB | 仅 Ask\|Build；上下文条显示当前 Issue；可「仅用项目」 | ⚠️ partial |
| P1-3 | Ask 只读提示 | Ask 模式看输入区文案 | 提示只读 / 写操作切 Build | ✅ code |
| P1-4 | 指派 Agent 持久 | 侧栏选 Agent → 指派 → 刷新 | Agent 仍在；状态可见；Dispatch 为次要按钮 | ✅ code |
| P1-5 | 评论 @Agent | 评论输入 `@` | 候选含成员+Agent（AI 标记）；提交后有回复或活动 | ✅ code |
| P1-6 | 活动流 Agent 审计 | Issue → 活动 | 有 Agent 区块/过滤，可见指派或执行记录 | ✅ code |
| P1-7 | Intake 分诊入口 | 项目设置 → 分诊 | TriagePanel 可开；AI 分析有返回或明确错误 | ⚠️ partial |
| P1-8 | 创建前去重 | 新建/快速创建相似标题 | 出现相似警告；可「仍要创建」且不拦截 | ✅ code |

---

## Phase 2

| # | 验收项 | 步骤 | 期望 | 结果 |
|---|--------|------|------|------|
| P2-1 | 安装开箱 Agent | 设置 → 自动化 → 安装开箱 Agent | 出现：请求分诊 / 交付风险 / Sprint 总结 / Spec 草稿（幂等再点不翻倍） | ✅ code |
| P2-2 | 创建→分诊模板 | 点「新建即分诊」模板 | 规则 trigger=`issue.created`；动作 `dispatch_agent` 指向请求分诊 | ⚠️ partial |
| P2-3 | 普通创建触发 | 启用规则后新建 Issue | Agent 活动或评论出现分诊建议（无 Key 则记「需 LLM」） | 🔲 needs live |
| P2-4 | Intake 触发 | 公网 Intake 提交一条 | 产生 pending Issue；同样触发分诊自动化 | 🔲 needs live |
| P2-5 | Cycle 一键总结 | Cycle 详情 → AI 总结 | 有结果面板；可存为 Page | ✅ code |
| P2-6 | Issue AI Tab | Issue → AI Tab → 总结 / 建议标签 | 页内出结果，不只打开空 Copilot；Copilot 按钮仍可用 | ⚠️ partial |

---

## 缺陷登记

| ID | 关联项 | 现象 | 严重度 | 状态 |
|----|--------|------|--------|------|
| DEF-01 | P2-2 | 自动化模板卡片仅在 `automations.length === 0` 时渲染 | P1 | **fixed** 2026-09-25 — 模板区常显 |
| DEF-02 | P2-6 | Issue AI Tab analyze 忽略 `issue_id`（项目级） | P1 | open（B1 前可跟进） |
| DEF-03 | P2-2 | `AutomationRuleBuilder` 动作下拉无 `dispatch_agent` | P1 | open（B1 跟进） |
| DEF-04 | P1-2 | Issue 详情无 Ctrl+J | P2 | **fixed** — IssueDetail Ctrl+J |
| DEF-05 | P1-7 | TriagePanel 吞错 | P2 | **fixed** — toast |
| DEF-06 | — | AutomationManager underscore trigger | P2 | **fixed** — 点分 |
| DEF-07 | P1-4 | FE unassign URL 与 BE 不一致 | P1 | **fixed** — `DELETE …/assign-agent` |

---

## 签字

- 执行人：静态验收 + 缺陷修复轮  日期：2026-09-25  
- 结论：☐ 通过  ☑ 有条件通过（DEF-02/03 仍 open；P2-3/4 需 live）  ☐ 不通过  

---

## 静态验收记录

> 日期：2026-09-25 · 分支 `master` · 仅代码证据，未启服务 / 未调 LLM。

- **P1-1 ✅** — `AppSidebar.vue` `navItems` 无 Agents；`AISettingsPanel.vue` → `/workspace/:slug/agents`（`advancedAgentsPath`）。
- **P1-2 ⚠️** — `AICopilot.vue` Ask\|Build tabs、`contextLabel`、`clearViewContext` / `ai.contextProjectOnly`；`IssueDetail.vue` 传入 `issue-id`。缺口：Issue 详情无 Ctrl+J/FAB（见 DEF-04）。
- **P1-3 ✅** — `AICopilot.vue` Ask 模式 `t('ai.askReadOnlyHint')`（zh：「Ask 为只读查询…切 Build」）。
- **P1-4 ✅** — `IssuePropertySidebar.vue` 主按钮指派、次要边框 Dispatch；`issueAgentApi.assign` + `loadAgentStatus`（`IssueDetail.vue`）。
- **P1-5 ✅** — `CommentList.vue` members+agents、`kind === 'agent'` 显示 AI；`comment_service.go` → `HandleMention` / `DispatchAgent`。
- **P1-6 ✅** — `IssueTabActivity.vue` Agent 区块 + `AgentAuditLog`（按 `issueId` 过滤）。
- **P1-7 ⚠️** — `ProjectSettings` section `triage` → `TriagePanel`；`ai-analyze` 有返回绑定，但失败无 toast（DEF-05）。
- **P1-8 ✅** — `IssueCreate.vue` / `QuickCreateInput.vue`：`checkDuplicates` + `issue.createAnyway` / 仍要创建。
- **P2-1 ✅** — `ensurePMAgents` → `POST .../agents/ensure-pm`；`pm_agents.go` 四名（请求分诊/交付风险/Sprint 总结/Spec 草稿）按 name 幂等；`ProjectSettings.installPMAgents`。
- **P2-2 ⚠️** — 模板 `intakeTriage`：`trigger: 'issue.created'` + `dispatch_agent` → `请求分诊`（`applyTemplate`）。缺口：仅空列表可见（DEF-01）；RuleBuilder 无 dispatch（DEF-03）。
- **P2-3 🔲** — 代码路径：`IssueService.NotifyIssueCreated` → `issue.created` + `dispatch_agent` handler；需 live/LLM 确认活动或评论。
- **P2-4 🔲** — `IntakeHandler.Submit` 建 pending + `NotifyIssueCreated`；需 live 确认端到端分诊。
- **P2-5 ✅** — `CycleDetail.vue`：`sprintPlan` 结果面板 + `saveAsPage`。
- **P2-6 ⚠️** — AI Tab 页内 `analyzeWithAI` / `suggestLabels` + Copilot 按钮；总结/风险实为项目级 Analyze（DEF-02）。`suggest` 标签路径正确。
