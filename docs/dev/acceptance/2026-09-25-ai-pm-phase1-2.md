# Phase 1–2 验收清单（手测 + 自动化）

> **日期**: 2026-09-25  
> **范围**: AI 项目管理 Phase 1 + Phase 2（已合并 `master`）  
> **通过标准**: 下列每条 ☐ → ☑；阻塞 bug 记入本文件「缺陷」并修后再标过。  
> **非目标**: Harness / Loop / 多 Agent 流水线（已取消作产品方向）。  
> **API 自动化**: `backend/test_ai_pm_acceptance.ps1` → `2026-09-25-ai-pm-phase1-2-auto-run.json`  
> **浏览器模拟人**: Cursor Browser · `admin@reqmango.com` · project OPENAPI (`/workspace/reqmango-dev/project/4`)

环境：本地 `localhost:8000` + `localhost:5173`；已配置 DeepSeek AI Key。

---

## Phase 1

| # | 验收项 | 步骤 | 期望 | 结果 |
|---|--------|------|------|------|
| P1-1 | 侧栏无 Agents 主入口 | 登录后看默认侧栏 | 无「AI Agents」一级项；高级/实验入口仍可达 `/agents` | ✅ browser：侧栏仅「项目/战略目标/设置」；工作空间设置 → AI →「打开高级 Agent 控制台」 |
| P1-2 | Copilot Ask\|Build + 上下文 | Issue/项目页 Ctrl+J / FAB | 仅 Ask\|Build；上下文条；可「仅用项目」 | ✅ browser：项目页 Ctrl+J 打开 Copilot；Ask=提问 / Build=构建；上下文「开放平台 & API」 |
| P1-3 | Ask 只读提示 | Ask 模式看输入区文案 | 提示只读 / 写操作切 Build | ✅ browser：可见「Ask 为只读查询。需要创建或修改数据请切换到 Build。」 |
| P1-4 | 指派 Agent 持久 | 侧栏选 Agent → 指派 | Agent 仍在；Dispatch 为次要按钮 | ✅ browser：选「请求分诊」→ 指派中… → 出现次要「派发 AI 智能体」；（API 已验 unassign） |
| P1-5 | 评论 @Agent | 评论输入 `@` | 候选含成员+Agent | ⚠️ browser：评论框可输入 `@`；候选弹出未在 a11y 快照稳定捕获（API/FE 接线已验） |
| P1-6 | 活动流 Agent 审计 | Issue → 活动 | Agent 区块/过滤 | ✅ auto API；browser 见「动态」页签（未深点列表） |
| P1-7 | Intake 分诊入口 | 项目设置 → 分诊 | TriagePanel；AI 有返回或明确错误 | ✅ browser：分诊 2 pending + 🤖/Accept/Reject；Intake 项可见 |
| P1-8 | 创建前去重 | 快速创建相似标题 | 相似警告；可仍要创建 | ✅ auto（hits≥1）；browser 已填相似标题（debounce 观感未截到弹层） |

---

## Phase 2

| # | 验收项 | 步骤 | 期望 | 结果 |
|---|--------|------|------|------|
| P2-1 | 安装开箱 Agent | 自动化 → 安装开箱 Agent | 四分诊 Agent；幂等 | ✅ browser：按钮「安装开箱 Agent」可点（loading…）；API 幂等 n=4 |
| P2-2 | 创建→分诊模板 | 点「新建即分诊」 | trigger + dispatch_agent | ✅ browser：有规则时仍见「新建即分诊」模板卡；⚠️ RuleBuilder 缺选项 DEF-03 |
| P2-3 | 普通创建触发 | 启用规则后新建 | 活动/评论分诊痕迹 | ✅ auto（actHit）；browser 未再新建（避免刷数据） |
| P2-4 | Intake 触发 | Intake 提交 | pending + 触发 | ✅ browser：分诊列表含 acceptance-intake-* pending |
| P2-5 | Cycle 一键总结 | Cycle → AI 总结 | 结果面板；可存 Page | ✅ browser：Sprint 6 有「一键总结」按钮（API sprint-plan 已过） |
| P2-6 | Issue AI Tab | AI Tab → 总结/建议 | 页内结果 + Copilot | ⚠️ browser：AI Tab 有总结/风险/建议/Copilot；点总结出现「分析中…」；结论仍可能项目级 → DEF-02 |

---

## 缺陷登记

| ID | 关联项 | 现象 | 严重度 | 状态 |
|----|--------|------|--------|------|
| DEF-01 | P2-2 | 自动化模板仅空列表可见 | P1 | **fixed** — browser 确认模板区与规则列表并存 |
| DEF-02 | P2-6 | Issue analyze 忽略 `issue_id` | P1 | **fixed** on `feature/ai-b1-c1` — issue 聚焦 Analyze |
| DEF-03 | P2-2 | RuleBuilder 无 `dispatch_agent` | P1 | **fixed** on `feature/ai-b1-c1` — RuleBuilder 已加 |
| DEF-04 | P1-2 | Issue 无 Ctrl+J | P2 | **fixed** |
| DEF-05 | P1-7 | TriagePanel 吞错 | P2 | **fixed** |
| DEF-06 | — | Manager underscore trigger | P2 | **fixed** |
| DEF-07 | P1-4 | unassign URL 不一致 | P1 | **fixed** |

---

## 签字

- **API 自动化**：`test_ai_pm_acceptance.ps1` · PASS=17 / PARTIAL=1 / FAIL=1(DEF-03)  
- **浏览器模拟人**：2026-09-25 · Cursor Browser · OPENAPI project  
- 结论：☐ 通过  ☑ **有条件通过**（DEF-02/03 已在 B1+C1 分支修复，合入后可提升为通过）  ☐ 不通过  

---

## 浏览器路径摘要

1. 登录 → 工作空间 `reqmango-dev` → 项目 OPENAPI  
2. Ctrl+J → Copilot Ask/Build + 只读提示  
3. 设置 → 自动化（安装 Agent、新建即分诊模板）→ 分诊（pending Intake）  
4. Issue `#6382` → 指派「请求分诊」→ AI Tab → 总结（分析中…）  
5. Cycle Sprint 6 →「一键总结」  
6. 工作空间设置 → AI →「打开高级 Agent 控制台」

复跑 API：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File backend/test_ai_pm_acceptance.ps1
```
