# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: login.test.ts >> 登录页面测试 >> 错误密码登录
- Location: tests\login.test.ts:20:3

# Error details

```
Error: expect(locator).toContainText(expected) failed

Locator: locator('div.bg-red-50')
Expected substring: "登录失败，请检查邮箱和密码"
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toContainText" with timeout 5000ms
  - waiting for locator('div.bg-red-50')

```

```yaml
- heading "Reqman AI" [level=1]
- paragraph: 登录您的账户
- text: 邮箱
- textbox "请输入邮箱"
- text: 密码
- textbox "请输入密码"
- button "登录"
- paragraph:
  - text: 还没有账户？
  - link "注册":
    - /url: /register
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test'
  2  | 
  3  | test.describe('登录页面测试', () => {
  4  |   test('页面加载正常', async ({ page }) => {
  5  |     await page.goto('/')
  6  |     
  7  |     await expect(page).toHaveTitle('Reqman AI - Project Management')
  8  |     await expect(page.locator('h1')).toContainText('Reqman AI')
  9  |     await expect(page.locator('p:text("登录您的账户")')).toBeVisible()
  10 |   })
  11 | 
  12 |   test('空表单提交验证', async ({ page }) => {
  13 |     await page.goto('/')
  14 |     
  15 |     await page.locator('button[type="submit"]').click()
  16 |     
  17 |     await expect(page.locator('div.bg-red-50')).toContainText('请填写所有字段')
  18 |   })
  19 | 
  20 |   test('错误密码登录', async ({ page }) => {
  21 |     await page.goto('/')
  22 |     
  23 |     await page.locator('input[type="email"]').fill('test@example.com')
  24 |     await page.locator('input[type="password"]').fill('wrongpassword')
  25 |     await Promise.all([
  26 |       page.waitForNavigation(),
  27 |       page.locator('button[type="submit"]').click()
  28 |     ])
  29 |     
> 30 |     await expect(page.locator('div.bg-red-50')).toContainText('登录失败，请检查邮箱和密码')
     |                                                 ^ Error: expect(locator).toContainText(expected) failed
  31 |   })
  32 | 
  33 |   test('成功登录', async ({ page }) => {
  34 |     await page.goto('/')
  35 |     
  36 |     await page.locator('input[type="email"]').fill('test@example.com')
  37 |     await page.locator('input[type="password"]').fill('test123456')
  38 |     await page.locator('button[type="submit"]').click()
  39 |     
  40 |     await page.waitForNavigation()
  41 |     await expect(page).toHaveURL('/')
  42 |   })
  43 | 
  44 |   test('跳转到注册页面', async ({ page }) => {
  45 |     await page.goto('/')
  46 |     
  47 |     await page.locator('text=注册').click()
  48 |     
  49 |     await page.waitForNavigation()
  50 |     await expect(page).toHaveURL('/register')
  51 |   })
  52 | })
```